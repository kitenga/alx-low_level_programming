#include <stdio.h>
/**
 * main - This program uses 'printf' to print a message
 * followed by a new line.
 * Return: 0 (Success)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n")
return (0);
}
